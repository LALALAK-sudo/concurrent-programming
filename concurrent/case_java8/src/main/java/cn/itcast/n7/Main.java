package cn.itcast.n7;

import java.util.*;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt(); // n 行
        HashMap<Integer, List> map = new HashMap<>();
        List<Integer> dpMax = new ArrayList<>();
        List<Integer> dpMaxLast = new ArrayList<>();
        dpMax.add(1);
        int index = 1;
        while(index <= n) {
            String str = sc.nextLine();
            int temp1 = 0;
            int temp2 = 0;
            if (str.startsWith("+")) {
            }
            index++;
        }
    }


}
